import { defineConfig } from 'vite';

export default defineConfig({
  // Use relative paths for assets so it works perfectly on GitHub Pages
  // regardless of the repository name (e.g., /PotasioGames/)
  base: './',
});
